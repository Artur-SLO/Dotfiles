-- Hyprland config (SlowerTrick)

--#####################
--## EXTERNAL FILES ###
--#####################

local env = require("components.env")
local monitors = require("components.monitors")
local autostart = require("components.autostart")
local workspaces = require("components.workspaces")
local window_rules = require("components.window_rules")
local appearence = require("components.appearence")
local input = require("components.input")
local binds = require("components.binds")

-- For Noctalia Color templates
local noctalia = require("noctalia")
noctalia.apply_theme()
